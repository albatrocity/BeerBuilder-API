<%= form_for(@beer_category) do |f| %>
  <% if @beer_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_category.errors.count, "error") %> prohibited this beer_category from being saved:</h2>

      <ul>
      <% @beer_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
