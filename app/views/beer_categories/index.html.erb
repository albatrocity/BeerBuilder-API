<h1>Listing beer_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @beer_categories.each do |beer_category| %>
  <tr>
    <td><%= beer_category.name %></td>
    <td><%= link_to 'Show', beer_category %></td>
    <td><%= link_to 'Edit', edit_beer_category_path(beer_category) %></td>
    <td><%= link_to 'Destroy', beer_category, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Beer category', new_beer_category_path %>
