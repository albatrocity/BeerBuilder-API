<h1>Listing ingredient_yeasts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ingredient_yeasts.each do |ingredient_yeast| %>
  <tr>
    <td><%= link_to 'Show', ingredient_yeast %></td>
    <td><%= link_to 'Edit', edit_ingredient_yeast_path(ingredient_yeast) %></td>
    <td><%= link_to 'Destroy', ingredient_yeast, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ingredient yeast', new_ingredient_yeast_path %>
