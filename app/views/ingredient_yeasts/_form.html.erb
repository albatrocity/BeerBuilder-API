<%= form_for(@ingredient_yeast) do |f| %>
  <% if @ingredient_yeast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_yeast.errors.count, "error") %> prohibited this ingredient_yeast from being saved:</h2>

      <ul>
      <% @ingredient_yeast.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
